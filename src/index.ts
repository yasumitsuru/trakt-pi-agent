// Trakt Pi Agent — minimal entrypoint
// Does NOT start the MCP server. Import-safe, exits cleanly, writes nothing to stdout.

export function main(): void {
  // Reserved for future initialization.
}
